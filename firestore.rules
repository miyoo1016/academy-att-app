rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TODO: 운영 전 관리자/단말 인증 기반 규칙으로 제한하세요.
      allow read, write: if true;
    }
  }
}
